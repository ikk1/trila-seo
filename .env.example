# .env.example — Trila SEO

# Conexão com o schema seo.* do PostgreSQL (Plano B).
# Ausente em dev → loadAllCities() usa o catálogo local lib/cities.ts.
# SEO_DB_URL=postgresql://user:pass@host:5432/beautyhub?schema=seo

# Google Analytics 4 Measurement ID (G-XXXXXXX).
# Ausente → analytics não carrega (degradação silenciosa).
NEXT_PUBLIC_GA_ID=
